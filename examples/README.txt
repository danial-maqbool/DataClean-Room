All sample records are synthetic. example.test is used instead of a real email domain.
Scan the contact and message files to review pattern matches.
The PNG contains a metadata comment. Cleaning removes the comment but leaves pixels.
A clean text export is not a redacted copy of the original PDF or Office layout.
Always review the output before sharing it. Pattern scans cannot find all private data.
